{
  "name": "ae-portkit",
  "version": "0.4.0",
  "private": true,
  "description": "PortKit Claude Code plugin — workflow + deterministic-helper unit tests",
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
